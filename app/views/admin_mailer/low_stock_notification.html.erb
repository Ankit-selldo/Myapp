<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; }
      .container { max-width: 600px; margin: 0 auto; padding: 20px; }
      .alert { color: #721c24; background-color: #f8d7da; border: 1px solid #f5c6cb; padding: 15px; margin-bottom: 20px; border-radius: 4px; }
      .product-info { margin: 20px 0; }
      .action-needed { margin-top: 30px; font-weight: bold; }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="alert">
        <h2>Low Stock Alert</h2>
        <p>The following product variant is running low on stock:</p>
      </div>

      <div class="product-info">
        <h3>Product Details:</h3>
        <p>
          <strong>Product:</strong> <%= @product.name %><br>
          <strong>Variant:</strong> <%= @variant.color %> / <%= @variant.size %><br>
          <strong>Current Stock:</strong> <%= @variant.stock %> units
        </p>
      </div>

      <div class="action-needed">
        <p>Please review and restock if necessary.</p>
      </div>
    </div>
  </body>
</html> 